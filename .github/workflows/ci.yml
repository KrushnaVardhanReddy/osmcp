name: CI

on:
  push:
    branches: [ "feature/dev", "main" ]
  pull_request:
    branches: [ "feature/dev", "main" ]

jobs:
  test:
    name: Build, Test & Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Build binary
        run: make build

      - name: Run unit tests
        run: go test -race ./internal/... ./docs/...

      - name: Run E2E tests
        run: make e2e

      - name: golangci-lint
        run: |
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.60.3
          golangci-lint run ./...
