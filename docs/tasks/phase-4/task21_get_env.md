# Task 21: `get_env` (Safe Environment Variables)

## Goal
Implement a read-only tool that returns the workspace environment variables, strictly filtering out any variables that might contain secrets, tokens, or passwords.

*(Spec and Contract will be defined when Phase 4 begins)*
