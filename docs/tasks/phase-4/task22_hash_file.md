# Task 22: `hash_file` (Cryptographic Digests)

## Goal
Implement a read-only tool that returns the cryptographic hash (SHA256, MD5) of a file to allow agents to verify file integrity without reading the entire contents into memory.

*(Spec and Contract will be defined when Phase 4 begins)*
